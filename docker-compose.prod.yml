version: '3'

networks:
  default:
    driver: bridge
services:
  nextjs_front:
    command: bash -c "npm run start"
  php_fpm:
    volumes:
      - "./docker/php/php.ini-production:/usr/local/etc/php/php.ini"